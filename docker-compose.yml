#version: "3.9"

services:
  agent-core:
    build:
      context: ./agent_core
      dockerfile: Dockerfile
    container_name: agent-core
    env_file: .env
    ports:
      - "${AGENT_CORE_PORT}:8004"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8004/v1/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - matprop_net
  agents:
      build:
        context: ./agents
        dockerfile: Dockerfile
      container_name: agents
      env_file: .env
      ports:
        - "${AGENTS_PORT}:8003"
      restart: unless-stopped
      volumes:
        - ollama_data:/root/.ollama
      depends_on:
        - agent-core
      healthcheck:
        test: ["CMD-SHELL", "curl -f http://localhost:8003/v1/health || exit 1"]
        interval: 30s
        timeout: 5s
        retries: 3
      deploy:
        resources:
          reservations:
            devices:
              - driver: nvidia
                count: all
                capabilities: [gpu]
      networks:
        - matprop_net

  #backend-ml:
  #  build:
  #    context: ./backend_ml
  #    dockerfile: Dockerfile
  #  container_name: backend-ml
  #  env_file: .env
  #  ports:
  #    - "${BACKEND_ML_PORT}:8000"
  #  volumes:
  #    - ./backend_llm/src/data:/app/data:ro
  #    - ./backend_ml/src:/app/src:ro
  #  depends_on:
  #    - llm-model
  #  restart: unless-stopped
  #  healthcheck:
  #    test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
  #    interval: 30s
  #    timeout: 5s
  #    retries: 3

  backend-llm:
    build:
      context: ./backend_llm
      dockerfile: Dockerfile
    container_name: backend-llm
    ports:
      - "${BACKEND_LLM_PORT}:8001"
    volumes:
      - ./backend_llm/src:/app:ro
    #depends_on:
      #- llamat2-chat
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8001/v1/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - matprop_net


  #llamat2-chat:
  #  build:
  #    context: ./llamat2-chat
  #    dockerfile: Dockerfile
  #  container_name: llamat2-chat
  #  env_file: .env
  #  ports:
  #    - "${LLAMAT2_CHAT_PORT}:8002"
    # Soporte GPU si existe (NVIDIA runtime)
  #  deploy:
  #    resources:
  #      reservations:
  #        devices:
  #          - driver: nvidia
  #            count: all
  #            capabilities: [gpu]
  #  restart: unless-stopped
  #  healthcheck:
  #    test: ["CMD-SHELL", "curl -f http://localhost:8002/v1/health || exit 1"]
  #    interval: 20s
  #    timeout: 5s
  #    retries: 5
  #  networks:
  #    - matprop_net
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: "http://localhost:8004"
        INTERNAL_API_URL: "http://agent-core:8004"
    container_name: frontend
    ports:
      - "${FRONTEND_PORT}:3000"
    environment:
      NEXT_PUBLIC_API_URL: "http://localhost:8004"
      INTERNAL_API_URL: "http://agent-core:8004"
    depends_on:
      - agent-core
    networks:
      - matprop_net

networks:
  matprop_net:
    driver: bridge


volumes:
  ollama_data:
